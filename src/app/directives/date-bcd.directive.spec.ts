import { DateBcdDirective } from './date-bcd.directive';

describe('DateBcdDirective', () => {
  it('should create an instance', () => {
    const directive = new DateBcdDirective();
    expect(directive).toBeTruthy();
  });
});
